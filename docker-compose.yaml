# SPDX-License-Identifier: Unlicense
services:
  mcp-bridge:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    # Example: pass specific arguments
    # command: ["--verbose", "--max-connections", "20"]
    restart: unless-stopped
